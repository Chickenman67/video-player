{
  "name": "video-player",
  "version": "1.0.0",
  "description": "Modern video player with embedded subtitle extraction",
  "main": "server/server.js",
  "scripts": {
    "start": "bash bootstrap.sh",
    "dev": "cd server && npm install && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "fluent-ffmpeg": "^2.1.2"
  },
  "engines": {
    "node": "18.x"
  }
}
